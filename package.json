{
  "name": "codex-ui-loop-mvp",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "dev:backend": "pnpm --filter backend dev",
    "dev:mcp": "pnpm --filter mcp-bridge dev"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
